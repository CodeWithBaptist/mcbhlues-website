"use client";

import { ThemeToggle } from "./theme-toggle";

/** Portal and public navigation use the same control and preference. */
export function PortalThemeToggle() {
  return <ThemeToggle />;
}
